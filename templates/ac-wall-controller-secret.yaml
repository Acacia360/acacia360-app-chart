# AC Wall Controller Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-ac-wall-controller-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.acWallController.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.acWallController.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.acWallController.secret.JWT_SECRET }}"