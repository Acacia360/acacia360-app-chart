# AHU Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-ahu
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: ahu-service
spec:
  replicas: {{ .Values.ahu.replicaCount }}
  selector:
    matchLabels:
      app: ahu-service
  template:
    metadata:
      labels:
        app: ahu-service
    spec:
      containers:
        - name: ahu-service
          image: "{{ .Values.ahu.image.repository }}:{{ .Values.ahu.image.tag }}"
          imagePullPolicy: {{ .Values.ahu.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.ahu.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-ahu-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-ahu-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-ahu-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-ahu-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-ahu-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.ahu.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.ahu.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.ahu.resources | nindent 12 }}
