# AHU Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-ahu-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.ahu.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.ahu.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.ahu.secret.JWT_SECRET }}"