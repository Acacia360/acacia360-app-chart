# Area Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-area-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.area.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.area.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.area.secret.JWT_SECRET }}"