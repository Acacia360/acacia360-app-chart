# Boiler Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-boiler
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: boiler-service
spec:
  replicas: {{ .Values.boiler.replicaCount }}
  selector:
    matchLabels:
      app: boiler-service
  template:
    metadata:
      labels:
        app: boiler-service
    spec:
      containers:
        - name: boiler-service
          image: "{{ .Values.boiler.image.repository }}:{{ .Values.boiler.image.tag }}"
          imagePullPolicy: {{ .Values.boiler.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.boiler.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-boiler-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-boiler-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-boiler-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-boiler-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-boiler-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.boiler.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.boiler.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.boiler.resources | nindent 12 }}
