# Boiler Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-boiler-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.boiler.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.boiler.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.boiler.secret.JWT_SECRET }}"