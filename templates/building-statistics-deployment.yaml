# Building Statistics Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-building-statistics
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: building-statistics-service
spec:
  replicas: {{ .Values.buildingStatistics.replicaCount }}
  selector:
    matchLabels:
      app: building-statistics-service
  template:
    metadata:
      labels:
        app: building-statistics-service
    spec:
      containers:
        - name: building-statistics-service
          image: "{{ .Values.buildingStatistics.image.repository }}:{{ .Values.buildingStatistics.image.tag }}"
          imagePullPolicy: {{ .Values.buildingStatistics.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.buildingStatistics.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-building-statistics-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-building-statistics-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-building-statistics-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-building-statistics-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-building-statistics-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.buildingStatistics.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.buildingStatistics.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.buildingStatistics.resources | nindent 12 }}
