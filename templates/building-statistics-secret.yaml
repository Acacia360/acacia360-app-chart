# Building Statistics Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-building-statistics-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.buildingStatistics.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.buildingStatistics.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.buildingStatistics.secret.JWT_SECRET }}"