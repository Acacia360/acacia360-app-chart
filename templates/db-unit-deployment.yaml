# DB Unit Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-db-unit
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: db-unit-service
spec:
  replicas: {{ .Values.dbUnit.replicaCount }}
  selector:
    matchLabels:
      app: db-unit-service
  template:
    metadata:
      labels:
        app: db-unit-service
    spec:
      containers:
        - name: db-unit-service
          image: "{{ .Values.dbUnit.image.repository }}:{{ .Values.dbUnit.image.tag }}"
          imagePullPolicy: {{ .Values.dbUnit.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.dbUnit.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-db-unit-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-db-unit-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-db-unit-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-db-unit-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-db-unit-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.dbUnit.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.dbUnit.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.dbUnit.resources | nindent 12 }}
