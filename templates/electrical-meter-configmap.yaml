# Electrical Meter Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-electrical-meter-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.electricalMeter.config.PORT }}"
  DB_HOST: "{{ .Values.electricalMeter.config.DB_HOST }}"
  DB_PORT: "{{ .Values.electricalMeter.config.DB_PORT }}"
  DB_NAME: "{{ .Values.electricalMeter.config.DB_NAME }}"
