# Electrical Meter Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-electrical-meter
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: electrical-meter-service
spec:
  replicas: {{ .Values.electricalMeter.replicaCount }}
  selector:
    matchLabels:
      app: electrical-meter-service
  template:
    metadata:
      labels:
        app: electrical-meter-service
    spec:
      containers:
        - name: electrical-meter-service
          image: "{{ .Values.electricalMeter.image.repository }}:{{ .Values.electricalMeter.image.tag }}"
          imagePullPolicy: {{ .Values.electricalMeter.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.electricalMeter.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-electrical-meter-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-electrical-meter-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-electrical-meter-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-electrical-meter-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-electrical-meter-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.electricalMeter.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.electricalMeter.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.electricalMeter.resources | nindent 12 }}
