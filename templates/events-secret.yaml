# Events Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-events-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.events.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.events.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.events.secret.JWT_SECRET }}"