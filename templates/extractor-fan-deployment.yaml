# Extractor Fan Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-extractor-fan
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: extractor-fan-service
spec:
  replicas: {{ .Values.extractorFan.replicaCount }}
  selector:
    matchLabels:
      app: extractor-fan-service
  template:
    metadata:
      labels:
        app: extractor-fan-service
    spec:
      containers:
        - name: extractor-fan-service
          image: "{{ .Values.extractorFan.image.repository }}:{{ .Values.extractorFan.image.tag }}"
          imagePullPolicy: {{ .Values.extractorFan.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.extractorFan.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-extractor-fan-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-extractor-fan-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-extractor-fan-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-extractor-fan-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-extractor-fan-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.extractorFan.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.extractorFan.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.extractorFan.resources | nindent 12 }}
