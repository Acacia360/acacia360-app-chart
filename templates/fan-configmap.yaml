# Fan Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-fan-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.fan.config.PORT }}"
  DB_HOST: "{{ .Values.fan.config.DB_HOST }}"
  DB_PORT: "{{ .Values.fan.config.DB_PORT }}"
  DB_NAME: "{{ .Values.fan.config.DB_NAME }}"
