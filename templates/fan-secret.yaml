# Fan Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-fan-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.fan.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.fan.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.fan.secret.JWT_SECRET }}"