# FCU Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-fcu-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.fcu.config.PORT }}"
  DB_HOST: "{{ .Values.fcu.config.DB_HOST }}"
  DB_PORT: "{{ .Values.fcu.config.DB_PORT }}"
  DB_NAME: "{{ .Values.fcu.config.DB_NAME }}"
