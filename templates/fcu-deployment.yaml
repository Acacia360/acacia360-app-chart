# FCU Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-fcu
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: fcu-service
spec:
  replicas: {{ .Values.fcu.replicaCount }}
  selector:
    matchLabels:
      app: fcu-service
  template:
    metadata:
      labels:
        app: fcu-service
    spec:
      containers:
        - name: fcu-service
          image: "{{ .Values.fcu.image.repository }}:{{ .Values.fcu.image.tag }}"
          imagePullPolicy: {{ .Values.fcu.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.fcu.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-fcu-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-fcu-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-fcu-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-fcu-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-fcu-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.fcu.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.fcu.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.fcu.resources | nindent 12 }}
