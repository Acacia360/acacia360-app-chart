# FCU Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-fcu-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.fcu.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.fcu.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.fcu.secret.JWT_SECRET }}"