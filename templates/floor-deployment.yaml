# Floor Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-floor
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: floor-service
spec:
  replicas: {{ .Values.floor.replicaCount }}
  selector:
    matchLabels:
      app: floor-service
  template:
    metadata:
      labels:
        app: floor-service
    spec:
      containers:
        - name: floor-service
          image: "{{ .Values.floor.image.repository }}:{{ .Values.floor.image.tag }}"
          imagePullPolicy: {{ .Values.floor.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.floor.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-floor-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-floor-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-floor-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-floor-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-floor-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.floor.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.floor.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.floor.resources | nindent 12 }}
