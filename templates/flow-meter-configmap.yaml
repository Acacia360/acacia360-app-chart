# Flow Meter Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-flow-meter-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.flowMeter.config.PORT }}"
  DB_HOST: "{{ .Values.flowMeter.config.DB_HOST }}"
  DB_PORT: "{{ .Values.flowMeter.config.DB_PORT }}"
  DB_NAME: "{{ .Values.flowMeter.config.DB_NAME }}"
