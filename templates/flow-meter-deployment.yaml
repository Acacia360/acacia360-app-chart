# Flow Meter Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-flow-meter
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: flow-meter-service
spec:
  replicas: {{ .Values.flowMeter.replicaCount }}
  selector:
    matchLabels:
      app: flow-meter-service
  template:
    metadata:
      labels:
        app: flow-meter-service
    spec:
      containers:
        - name: flow-meter-service
          image: "{{ .Values.flowMeter.image.repository }}:{{ .Values.flowMeter.image.tag }}"
          imagePullPolicy: {{ .Values.flowMeter.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.flowMeter.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-flow-meter-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-flow-meter-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-flow-meter-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-flow-meter-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-flow-meter-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.flowMeter.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.flowMeter.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.flowMeter.resources | nindent 12 }}
