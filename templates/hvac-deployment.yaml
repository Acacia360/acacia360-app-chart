# HVAC Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-hvac
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: hvac-service
spec:
  replicas: {{ .Values.hvac.replicaCount }}
  selector:
    matchLabels:
      app: hvac-service
  template:
    metadata:
      labels:
        app: hvac-service
    spec:
      containers:
        - name: hvac-service
          image: "{{ .Values.hvac.image.repository }}:{{ .Values.hvac.image.tag }}"
          imagePullPolicy: {{ .Values.hvac.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.hvac.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-hvac-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-hvac-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-hvac-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-hvac-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-hvac-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.hvac.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.hvac.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.hvac.resources | nindent 12 }}
