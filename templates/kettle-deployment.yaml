# Kettle Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-kettle
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: kettle-service
spec:
  replicas: {{ .Values.kettle.replicaCount }}
  selector:
    matchLabels:
      app: kettle-service
  template:
    metadata:
      labels:
        app: kettle-service
    spec:
      containers:
        - name: kettle-service
          image: "{{ .Values.kettle.image.repository }}:{{ .Values.kettle.image.tag }}"
          imagePullPolicy: {{ .Values.kettle.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.kettle.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-kettle-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-kettle-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-kettle-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-kettle-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-kettle-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.kettle.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.kettle.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.kettle.resources | nindent 12 }}
