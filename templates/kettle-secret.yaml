# Kettle Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-kettle-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.kettle.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.kettle.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.kettle.secret.JWT_SECRET }}"