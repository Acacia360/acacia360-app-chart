# Light Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-light-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.light.config.PORT }}"
  DB_HOST: "{{ .Values.light.config.DB_HOST }}"
  DB_PORT: "{{ .Values.light.config.DB_PORT }}"
  DB_NAME: "{{ .Values.light.config.DB_NAME }}"
