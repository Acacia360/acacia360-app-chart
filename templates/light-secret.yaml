# Light Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-light-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.light.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.light.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.light.secret.JWT_SECRET }}"