# Luminaire Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-luminaire
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: luminaire-service
spec:
  replicas: {{ .Values.luminaire.replicaCount }}
  selector:
    matchLabels:
      app: luminaire-service
  template:
    metadata:
      labels:
        app: luminaire-service
    spec:
      containers:
        - name: luminaire-service
          image: "{{ .Values.luminaire.image.repository }}:{{ .Values.luminaire.image.tag }}"
          imagePullPolicy: {{ .Values.luminaire.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.luminaire.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-luminaire-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-luminaire-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-luminaire-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-luminaire-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-luminaire-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.luminaire.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.luminaire.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.luminaire.resources | nindent 12 }}
