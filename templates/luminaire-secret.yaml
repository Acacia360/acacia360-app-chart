# Luminaire Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-luminaire-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.luminaire.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.luminaire.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.luminaire.secret.JWT_SECRET }}"