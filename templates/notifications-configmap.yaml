# Notifications Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-notifications-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.notifications.config.PORT }}"
  DB_HOST: "{{ .Values.notifications.config.DB_HOST }}"
  DB_PORT: "{{ .Values.notifications.config.DB_PORT }}"
  DB_NAME: "{{ .Values.notifications.config.DB_NAME }}"
