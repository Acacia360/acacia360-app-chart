# Notifications Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-notifications-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.notifications.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.notifications.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.notifications.secret.JWT_SECRET }}"