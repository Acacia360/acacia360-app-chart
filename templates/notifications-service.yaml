# Notifications Service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-notifications-service
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: notifications-service
spec:
  type: {{ .Values.notifications.service.type }}
  selector:
    app: notifications-service
  ports:
    - protocol: TCP
      port: {{ .Values.notifications.service.port }}
      targetPort: {{ .Values.notifications.service.targetPort }}
