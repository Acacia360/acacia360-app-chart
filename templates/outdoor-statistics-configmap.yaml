# Outdoor Statistics Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-outdoor-statistics-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.outdoorStatistics.config.PORT }}"
  DB_HOST: "{{ .Values.outdoorStatistics.config.DB_HOST }}"
  DB_PORT: "{{ .Values.outdoorStatistics.config.DB_PORT }}"
  DB_NAME: "{{ .Values.outdoorStatistics.config.DB_NAME }}"