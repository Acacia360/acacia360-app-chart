# Outdoor Statistics Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-outdoor-statistics
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: outdoor-statistics-service
spec:
  replicas: {{ .Values.outdoorStatistics.replicaCount }}
  selector:
    matchLabels:
      app: outdoor-statistics-service
  template:
    metadata:
      labels:
        app: outdoor-statistics-service
    spec:
      containers:
        - name: outdoor-statistics-service
          image: "{{ .Values.outdoorStatistics.image.repository }}:{{ .Values.outdoorStatistics.image.tag }}"
          imagePullPolicy: {{ .Values.outdoorStatistics.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.outdoorStatistics.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-outdoor-statistics-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-outdoor-statistics-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-outdoor-statistics-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-outdoor-statistics-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-outdoor-statistics-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.outdoorStatistics.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.outdoorStatistics.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.outdoorStatistics.resources | nindent 12 }}
