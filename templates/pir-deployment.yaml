# PIR Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-pir
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: pir-service
spec:
  replicas: {{ .Values.pir.replicaCount }}
  selector:
    matchLabels:
      app: pir-service
  template:
    metadata:
      labels:
        app: pir-service
    spec:
      containers:
        - name: pir-service
          image: "{{ .Values.pir.image.repository }}:{{ .Values.pir.image.tag }}"
          imagePullPolicy: {{ .Values.pir.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.pir.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-pir-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-pir-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-pir-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-pir-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-pir-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.pir.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.pir.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.pir.resources | nindent 12 }}
