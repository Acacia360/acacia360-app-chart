# PIR Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-pir-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.pir.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.pir.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.pir.secret.JWT_SECRET }}"