# Portfolio Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-portfolio-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.portfolio.config.PORT }}"
  DB_HOST: "{{ .Values.portfolio.config.DB_HOST }}"
  DB_PORT: "{{ .Values.portfolio.config.DB_PORT }}"
  DB_NAME: "{{ .Values.portfolio.config.DB_NAME }}"
