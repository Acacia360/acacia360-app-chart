# Portfolio Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-portfolio-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.portfolio.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.portfolio.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.portfolio.secret.JWT_SECRET }}"