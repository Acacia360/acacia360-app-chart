# Reports Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-reports-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.reports.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.reports.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.reports.secret.JWT_SECRET }}"