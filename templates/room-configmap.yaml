# Room Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-room-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.room.config.PORT }}"
  DB_HOST: "{{ .Values.room.config.DB_HOST }}"
  DB_PORT: "{{ .Values.room.config.DB_PORT }}"
  DB_NAME: "{{ .Values.room.config.DB_NAME }}"

  # Internal microservice URLs
  USER_SERVICE_URL: "{{ .Values.gateway.config.USER_SERVICE_URL }}"
