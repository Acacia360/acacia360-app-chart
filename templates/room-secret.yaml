# Room Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-room-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.room.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.room.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.room.secret.JWT_SECRET }}"