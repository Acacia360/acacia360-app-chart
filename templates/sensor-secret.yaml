# Sensor Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-sensor-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.sensor.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.sensor.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.sensor.secret.JWT_SECRET }}"