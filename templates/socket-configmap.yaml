# Socket Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-socket-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.socket.config.PORT }}"
  DB_HOST: "{{ .Values.socket.config.DB_HOST }}"
  DB_PORT: "{{ .Values.socket.config.DB_PORT }}"
  DB_NAME: "{{ .Values.socket.config.DB_NAME }}"
