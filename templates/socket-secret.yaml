# Socket Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-socket-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.socket.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.socket.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.socket.secret.JWT_SECRET }}"