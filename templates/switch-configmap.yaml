# Switch Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-switch-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.switch.config.PORT }}"
  DB_HOST: "{{ .Values.switch.config.DB_HOST }}"
  DB_PORT: "{{ .Values.switch.config.DB_PORT }}"
  DB_NAME: "{{ .Values.switch.config.DB_NAME }}"
