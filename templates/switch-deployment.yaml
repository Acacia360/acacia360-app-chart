# Switch Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-switch
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: switch-service
spec:
  replicas: {{ .Values.switch.replicaCount }}
  selector:
    matchLabels:
      app: switch-service
  template:
    metadata:
      labels:
        app: switch-service
    spec:
      containers:
        - name: switch-service
          image: "{{ .Values.switch.image.repository }}:{{ .Values.switch.image.tag }}"
          imagePullPolicy: {{ .Values.switch.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.switch.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-switch-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-switch-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-switch-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-switch-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-switch-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.switch.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.switch.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.switch.resources | nindent 12 }}
