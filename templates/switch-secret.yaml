# Switch Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-switch-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.switch.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.switch.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.switch.secret.JWT_SECRET }}"