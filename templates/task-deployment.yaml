# Task Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-task
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: task-service
spec:
  replicas: {{ .Values.task.replicaCount }}
  selector:
    matchLabels:
      app: task-service
  template:
    metadata:
      labels:
        app: task-service
    spec:
      initContainers:
      - name: wait-for-kafka
        image: busybox
        command:
          - sh
          - -c
          - |
            until nc -z kafka 9092; do
              echo "Waiting for kafka...";
              sleep 5;
            done
      containers:
        - name: task-service
          image: "{{ .Values.task.image.repository }}:{{ .Values.task.image.tag }}"
          imagePullPolicy: {{ .Values.task.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.task.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-task-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-task-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-task-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-task-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-task-secret
                  key: JWT_SECRET
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-task-config
                  key: KAFKA_BOOTSTRAP_SERVERS

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.task.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.task.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.task.resources | nindent 12 }}
