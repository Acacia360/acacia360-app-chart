# Telephone Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-telephone-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.telephone.config.PORT }}"
  DB_HOST: "{{ .Values.telephone.config.DB_HOST }}"
  DB_PORT: "{{ .Values.telephone.config.DB_PORT }}"
  DB_NAME: "{{ .Values.telephone.config.DB_NAME }}"
