# Telephone Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-telephone
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: telephone-service
spec:
  replicas: {{ .Values.telephone.replicaCount }}
  selector:
    matchLabels:
      app: telephone-service
  template:
    metadata:
      labels:
        app: telephone-service
    spec:
      containers:
        - name: telephone-service
          image: "{{ .Values.telephone.image.repository }}:{{ .Values.telephone.image.tag }}"
          imagePullPolicy: {{ .Values.telephone.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.telephone.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-telephone-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-telephone-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-telephone-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-telephone-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-telephone-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.telephone.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.telephone.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.telephone.resources | nindent 12 }}
