# Telephone Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-telephone-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.telephone.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.telephone.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.telephone.secret.JWT_SECRET }}"