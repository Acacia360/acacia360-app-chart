# Thermal Meter Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-thermal-meter
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: thermal-meter-service
spec:
  replicas: {{ .Values.thermalMeter.replicaCount }}
  selector:
    matchLabels:
      app: thermal-meter-service
  template:
    metadata:
      labels:
        app: thermal-meter-service
    spec:
      containers:
        - name: thermal-meter-service
          image: "{{ .Values.thermalMeter.image.repository }}:{{ .Values.thermalMeter.image.tag }}"
          imagePullPolicy: {{ .Values.thermalMeter.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.thermalMeter.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-thermal-meter-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-thermal-meter-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-thermal-meter-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-thermal-meter-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-thermal-meter-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.thermalMeter.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.thermalMeter.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.thermalMeter.resources | nindent 12 }}
