# Thermal Meter Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-thermal-meter-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.thermalMeter.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.thermalMeter.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.thermalMeter.secret.JWT_SECRET }}"