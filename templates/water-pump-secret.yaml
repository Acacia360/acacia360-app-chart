# Water Pump Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-water-pump-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.waterPump.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.waterPump.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.waterPump.secret.JWT_SECRET }}"