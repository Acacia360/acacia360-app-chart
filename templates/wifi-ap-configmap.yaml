# WiFi AP Configmap
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-wifi-ap-config
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
data:
  PORT: "{{ .Values.wifiAp.config.PORT }}"
  DB_HOST: "{{ .Values.wifiAp.config.DB_HOST }}"
  DB_PORT: "{{ .Values.wifiAp.config.DB_PORT }}"
  DB_NAME: "{{ .Values.wifiAp.config.DB_NAME }}"
