# WiFi AP Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-wifi-ap
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
    app: wifi-ap-service
spec:
  replicas: {{ .Values.wifiAp.replicaCount }}
  selector:
    matchLabels:
      app: wifi-ap-service
  template:
    metadata:
      labels:
        app: wifi-ap-service
    spec:
      containers:
        - name: wifi-ap-service
          image: "{{ .Values.wifiAp.image.repository }}:{{ .Values.wifiAp.image.tag }}"
          imagePullPolicy: {{ .Values.wifiAp.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.wifiAp.service.port }}

          envFrom:
            - configMapRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-wifi-ap-config
            - secretRef:
                name: {{ include "acacia360-app-chart.fullname" . }}-wifi-ap-secret

          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-wifi-ap-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-wifi-ap-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "acacia360-app-chart.fullname" . }}-wifi-ap-secret
                  key: JWT_SECRET

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.wifiAp.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.wifiAp.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          resources:
            {{- toYaml .Values.wifiAp.resources | nindent 12 }}
