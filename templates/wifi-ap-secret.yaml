# WiFi AP Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acacia360-app-chart.fullname" . }}-wifi-ap-secret
  labels:
    {{- include "acacia360-app-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  DB_USER: "{{ .Values.wifiAp.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.wifiAp.secret.DB_PASSWORD }}"
  JWT_SECRET: "{{ .Values.wifiAp.secret.JWT_SECRET }}"